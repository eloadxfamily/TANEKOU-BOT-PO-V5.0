# TANEKOU BOT PO — Dépendances v2.1
# websockets : large range — _ws_open() gère le fallback automatiquement
# pour toutes les versions 9.x → 13.x

python-telegram-bot==20.7
pandas==2.1.4
numpy==1.26.2
pandas-ta==0.3.14b0
python-dotenv==1.0.0
aiofiles==23.2.1
APScheduler==3.10.4
websockets>=9.0,<14.0
aiohttp==3.9.1
